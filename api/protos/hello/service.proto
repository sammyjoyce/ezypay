syntax = "proto3";

package hello;

import "messages.proto";

option go_package = "example/internal/gen/hello";

// The greeting service definition
service HelloService {
  // Sends a greeting
  rpc SayHello (HelloRequest) returns (HelloResponse);
}
