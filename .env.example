# {{project_name}} Environment Variables
# ======================================

# Project Information
PROJECT_NAME={{project_name}}
PROJECT_DESCRIPTION={{project_description}}
GO_MODULE={{go_module}}

# Node environment (development/production)
NODE_ENV=development

# Server Configuration
# ------------------
# API server port (default: 8080)
PORT=8080

# gRPC Configuration
# ----------------
# Host for gRPC server (use 0.0.0.0 to listen on all interfaces)
GRPC_HOST=0.0.0.0
# Port for gRPC server
GRPC_PORT=50051

# CORS Configuration
# ----------------
# Comma-separated list of allowed origins
# Development default: Vite development server
# Production: Override with your domain(s)
ALLOWED_ORIGINS=http://localhost:5173

# Vite Environment Variables
# ========================
# Note: All Vite env variables must be prefixed with VITE_

# API Configuration
# ---------------
# Host for the API server
VITE_API_HOST=localhost
# Port for the API server (should match PORT above)
VITE_API_PORT=8080

# gRPC Web Configuration
# --------------------
# Host for gRPC-Web client (should match GRPC_HOST for development)
VITE_GRPC_HOST=localhost
# Port for gRPC-Web client (should match GRPC_PORT above)
VITE_GRPC_PORT=50051
